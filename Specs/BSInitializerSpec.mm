#import <Cedar/SpecHelper.h>
#import "BSInitializer.h"

using namespace Cedar::Matchers;

SPEC_BEGIN(BSInitializerSpec)

describe(@"BSInitializer", ^{
//    __block BSInitializer *initializer;
    
    describe(@"arity", ^{
//        xit(@"returns the number of arguments in the selector", ^{
//            initializer = [BSInitializer initializer:@selector(foo), nil];
//            expect(initializer.arity).to(equal(0));                           
//            initializer = [BSInitializer initializer:@selector(foo:), nil];
//            expect(initializer.arity).to(equal(1));                           
//        });
    });
});

SPEC_END